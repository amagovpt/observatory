import { GetConformPipe } from './get-conform.pipe';

describe('GetConformPipe', () => {
  it('create an instance', () => {
    const pipe = new GetConformPipe();
    expect(pipe).toBeTruthy();
  });
});
